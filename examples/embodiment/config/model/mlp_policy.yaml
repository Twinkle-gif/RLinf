model_type: "mlp_policy"

model_path: ""

policy_setup: "panda-qpos"
obs_dim: 41 # distribute_obj (num_objects=1): agent_state(22) + tcp_pose(7) + obj_poses(1*7) + goal_positions(1*3) + tcp_to_objs(1*3) + obj_to_goal_dist(1*1) + is_grasping(1) - 计算式见 _get_obs_extra
# 旧值 39 对应未加 tcp_to_objs/obj_to_goal_dist/is_grasping 时的形状
action_dim: 8
num_action_chunks: 1
hidden_dim: 512

precision: "32"
add_value_head: True

is_lora: False
lora_rank: 32
